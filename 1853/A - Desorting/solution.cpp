#include <bits/stdc++.h>using namespace std; int main() {    int t;    cin >> t;    while (t--) {        int n;        cin >> n;        vector<long long int> a(n);        for (int i = 0; i < n; i++) {            cin >> a[i];        }        // if (!is_sorted(a.begin(), a.end())) {        //     cout << 0 << endl;        // }        // else {        //     long long int min_global = LLONG_MAX;        //     int min_idx = -1;        //     for (int i = 0; i < n - 1; i++ ) {        //         long long int diff = abs(a[i] - a[i + 1]);        //         if (diff < min_global) {        //             min_global = diff;        //             min_idx = i;        //         }        //     }        //     double min_val_needed = min_global/2.0;        //     if (min_val_needed >= 2) {        //         cout << ceil(min_val_needed) << endl;        //     }        //     else {        //         cout << (int)min_val_needed + 1 << endl;        //     }        // }        if (!is_sorted(a.begin(), a.end())) {            cout << 0 << endl;        } else {            long long ans = LLONG_MAX;            for (int i = 0; i < n - 1; i++) {                long long d = a[i + 1] - a[i];                ans = min(ans, d / 2 + 1);            }            cout << ans << endl;        }     }    return 0;}