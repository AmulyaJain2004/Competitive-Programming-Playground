#include <bits/stdc++.h>using namespace std; int main() {    int t;    cin >> t;    while (t--) {        int n;        cin >> n;        vector<int> a(n);        int cnt2 {0};         for (int i = 0; i < n; i++) {            cin >> a[i];            if (a[i] == 2) {                cnt2++;            }        }        if (cnt2 % 2 == 1) {            cout << -1 << endl;            continue;        }        int left2 {0};        for (int i = 0; i < n; i++) {            if (a[i] == 2) {                left2++;            }            if (left2 == cnt2 / 2) {                cout << i + 1 << endl;                break;            }        }    }    return 0;}