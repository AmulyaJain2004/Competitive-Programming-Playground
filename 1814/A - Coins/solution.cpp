#include <bits/stdc++.h>using namespace std; int main() {    int t;    cin >> t;    while (t--) {        long long n, k;        cin >> n >> k;        if (n % 2 == 0 && k % 2 == 0) {            cout << "YES" << endl;        }        else if (n % 2 == 0 && k % 2 == 1) {            if (n>=2*k || n >= 0) {                cout << "YES" << endl;            } else {                cout << "NO" << endl;            }        }        else if (n % 2 == 1 && k % 2 == 0) {            cout << "NO" << endl;        }        else {            if (n >= k || n >= 0) {                cout << "YES" << endl;            } else {                cout << "NO" << endl;            }        }         // my prev approach which is failing        // this is will give TLE for large n <= 10^18        // bool can = false;        // for (long long x = 0; x <= n/2; x++) {        //     long long y = n - 2*x;        //     if (y % k == 0) {        //         can = true;        //         break;        //     }        // }        // if (can) {        //     cout << "YES" << endl;        // } else {        //     cout << "NO" << endl;        // }    }    return 0;}