#include <bits/stdc++.h>using namespace std; int main() {    int t;    cin >> t;    while (t--) {        int n;        cin >> n;        // Simple apprach        vector<int> a(n-1);        int summ = 0;        for (int i = 0; i < n-1; i++) {            cin >> a[i];            summ += a[i];        }        cout << -1 * summ << "\n";    }    return 0;}