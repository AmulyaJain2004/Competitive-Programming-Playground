#include <bits/stdc++.h>using namespace std; int main() {    int t;    cin >> t;    while (t--) {        int n;        cin >> n;        // my approach        // int extreme_count {0};        // for (int i = 1; i <= n; i++) {        //     int numlen = (to_string(i)).size();        //     if (numlen == 1 || (numlen > 1 && i % (int)ceil(pow(10, numlen - 1)) == 0)) {        //         extreme_count++;        //     }        // }        // cout << extreme_count << "\n";         // optimized approach         // int extreme_count = 0;        // for (int base = 1; base <= n; base *= 10) {        //     for (int d = 1; d <= 9; d++) {        //         if (d * base <= n) ans++;        //     }        // }         // another approach most optimized        int extreme_count {0};        int first_digit = to_string(n)[0] - '0';        cout << 9 * (to_string(n).size() - 1) + first_digit << "\n";    }    return 0;}