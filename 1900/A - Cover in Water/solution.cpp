#include <bits/stdc++.h>using namespace std; int main() {    int t;    cin >> t;    while (t--) {        int n;        cin >> n;        string s;        cin >> s;        int cnt {0};        for (int i = 0; i < n; i++) {            if (s[i] == '.') {                cnt++;                if (i + 1 < n && s[i + 1] == '.') {                    if (i + 2 < n && s[i + 2] == '.') {                        cnt = 2;                        break;                    }                    cnt ++;                    i ++;                }            }            else {                continue;            }        }        cout << cnt << "\n";    }    return 0;}