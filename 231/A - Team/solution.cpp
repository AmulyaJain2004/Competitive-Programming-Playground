# include <bits/stdc++.h>using namespace std; int main() {    int n;    cin >> n;    int countDoable = 0;    while (n--) {        vector<int> canSolve(3);        int views = 0;        for (int i = 0; i < canSolve.size(); i++) {            cin >> canSolve[i];            views += canSolve[i];        }        if (views >= 2) {            countDoable ++;        }    }    cout << countDoable << endl;    return 0;}