#include <bits/stdc++.h>using namespace std; int main() {    int t;    cin >> t;    while (t--) {        long long int n;        cin >> n;        if (n % 2 == 1) { // n is already given greater than or equal to 2            cout << "YES" << endl;        }         else {            // Below tricky to think             while (n % 2 == 0) {                n /= 2;            }            if (n > 1) {                cout << "YES\n";            } else {                cout << "NO\n";            }        }    }    return 0;}