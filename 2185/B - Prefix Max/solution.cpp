#include <bits/stdc++.h>using namespace std; int main() {    int t;    cin >> t;    while (t--) {        int n;        cin >> n;        vector<int> a(n);        for (int i = 0; i < n; i++) {            cin >> a[i];        }        int max_elem {a[0]};        int j {0};        for (int i = 1; i < n; i++) {            if (a[i] >= max_elem) {                max_elem = a[i];                j = i;            }        }        swap(a[0], a[j]);        cout << a[0]*n << "\n";    }    return 0;}